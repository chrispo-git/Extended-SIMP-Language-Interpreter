pd reverseArr(a: Int[]) {
    i := 0;
    j := len(a) - 1;
    while !i < !j do {
        tmp := a[!i];
        a[!i] := a[!j];
        a[!j] := !tmp;
        i += 1;
        j -= 1;
    };
}

nums := [1, 2, 3, 4, 5];
print nums;
call reverseArr(nums);
print nums;