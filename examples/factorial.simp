fn factorial(n : Int) -> Int {
    if !n == 0 then {
        return 1;
    } else {
        return !n * factorial(!n - 1);
    };
}

myVal := inputInt("get factorial: ");
print "The Factorial of " + myVal + " is " + factorial(myVal);